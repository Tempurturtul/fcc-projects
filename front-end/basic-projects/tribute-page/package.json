{
  "name": "tribute-page",
  "version": "1.0.0",
  "description": "My take on Free Code Camp's Tribute Page project.",
  "scripts": {
    "build": "mkdir dist && mkdir dist/tribute-page && cp -r src/* dist/tribute-page",
    "clean": "rm -r dist",
    "predeploy": "npm run build",
    "deploy": "gh-pages --dist ./dist --add",
    "postdeploy": "npm run clean"
  },
  "author": "Matthew Feidt",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^0.12.0"
  }
}
